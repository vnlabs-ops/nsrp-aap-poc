---
- name: Copy CSV to web server
  hosts: web

  vars_files:
  - vars/aruba.yaml

  tasks:
    - name: Copy CSV to web server
      ansible.builtin.copy:
        src: "{{ aap_csv_file_path }}/{{ csv_filename }}"
        dest: "{{ web_html_file_path }}/{{ csv_filename }}"
    